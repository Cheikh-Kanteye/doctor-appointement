import { View, Text } from "react-native";
import React from "react";
import { AppContainer } from "@/components";

const home = () => {
  return (
    <AppContainer>
      <Text>home</Text>
    </AppContainer>
  );
};

export default home;
